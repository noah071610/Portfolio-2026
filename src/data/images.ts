const baseUrl = "https://noah071610.github.io/Portfolio-2026"

import { IMAGES } from "../generated/path/images"

export const imageAssets = {
  bg: baseUrl + IMAGES.BG.src,
  coffeeBeans: baseUrl + IMAGES.COFFEE_BEANS.src,
  doda: {
    squareLogo: baseUrl + IMAGES.DODA_SQUARE_LOGO.src,
  },
  thumbnail: {
    canape: baseUrl + IMAGES.CANAPE_THUMBNAIL.src,
    smore: baseUrl + IMAGES.SMORE_THUMBNAIL.src,
    rankingtogether: baseUrl + IMAGES.RANKINGTOGETHER_THUMBNAIL.src,
  },
  frame: baseUrl + IMAGES.FRAME.src,
  quiz: {
    thumbnail: baseUrl + IMAGES.THUMBNAIL.src,
    result: baseUrl + IMAGES.RESULT.src,
  },
  wordbook: {
    wordbook1: baseUrl + IMAGES.WORDBOOK_1.src,
    wordbook2: baseUrl + IMAGES.WORDBOOK_2.src,
    wordbook3: baseUrl + IMAGES.WORDBOOK_3.src,
    wordbook4: baseUrl + IMAGES.WORDBOOK_4.src,
  },
  profile: baseUrl + IMAGES.PROFILE.src,
  roulette: {
    candy: baseUrl + IMAGES.CANDY.src,
    coffee: baseUrl + IMAGES.COFFEE.src,
    coin: baseUrl + IMAGES.COIN.src,
    wine: baseUrl + IMAGES.WINE.src,
    money: baseUrl + IMAGES.MONEY.src,
    gift: baseUrl + IMAGES.GIFT.src,
  },
  shellGame: {
    cup: baseUrl + IMAGES.CUP.src,
  },
  tarotCard: {
    front: baseUrl + IMAGES.TAROT_CARD_FRONT.src,
    back: baseUrl + IMAGES.TAROT_CARD_BACK.src,
  },
  videos: {
    vn: `${baseUrl}/videos/vn.mp4`,
    play: `${baseUrl}/videos/play.mp4`,
    result: `${baseUrl}/videos/result.mp4`,
  },
}
